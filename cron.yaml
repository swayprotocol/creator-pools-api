cron:
# staging
- description: "Sync database long interval"
  url: /databaseSyncMinutes?minutes=500
  schedule: every 8 hours
  target: creatorpools-api-staging

  # Production
- description: "Sync database long interval"
  url: /databaseSyncMinutes?minutes=500
  schedule: every 8 hours
  target: default



